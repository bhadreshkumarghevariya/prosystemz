import { background, primary, secondary, accent3 } from "./colors";

export const primaryButtonStyle = {
  backgroundColor: primary,
  color: background,
  borderColor: primary,
  TextDecoder: {
    fontWeight: "bold",
  },
};
